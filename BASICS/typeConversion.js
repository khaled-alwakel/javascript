//* type conversion when we manually convert type to another

const inputYear = "1991";
console.log(Number(inputYear));
console.log(Number(inputYear) + 19);

console.log(Number("khaled"));

console.log(typeof NaN); // still a number some how but it's a not valid number

//? javascript can only convert to three types to a number or string or boolean
// * but cannot convert something to undefined or to null
